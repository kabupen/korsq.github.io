
# 基本動作

## データ入出力

```
data <- read.table(file="xxxx.txt", header=T, sep="\t")
```

- header：第一行目がヘッダーかどうか
- sep：区切り文字の指定

```
write.table(file="xxxx.txt", sep="\t")
```

## その他

- `?t.test`：?でマニュアルを見る

# 統計検定